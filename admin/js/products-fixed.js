/* ADMIN PRODUCTS - FIXED VERSION 
   Supabase timing + loading issues resolved
*/

let supabase = window.dalightAdminSupabase || window.supabaseClient;

// Global state
let allProducts = [];
let allCategories
